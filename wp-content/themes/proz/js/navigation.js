/**
 * File navigation.js.
 *
 * Handles toggling the navigation menu for small screens and enables TAB key
 * navigation support for dropdown menus.
 */
( function() {
    $(document).ready(function() {
        // $(document).on('click', '#masthead .eael-simple-menu-toggle', function(e) {
        //     e.preventDefault();
        //     var $this = $(this);

        // });
    });
}(jQuery) );
